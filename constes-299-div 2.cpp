#include <iostream>
#include <vector>
#include <stdio.h>
#include <algorithm>
#include <string.h>
#include <string>
using namespace std;
#define FOR(i,a,b) for(int i=(a), _i=(b);i<_i; i++)
#define pb push_back
#define size 100

int main() {
	int N;
	if(N==1){
		cout<<"one"<<endl;
	}
	return 0;
}
