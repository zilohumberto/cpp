#include <iostream>
#include <set>
#include <algorithm>
using namespace std;
#define FOR(i,a,b) for(int i = (a), _i=(b); i<_i; i++)
int main() {
	int N,x,y,a;
	/*cin>>N;
	cin>>x;
	set<int> guy;
	FOR(i,0,x){ cin>>a; guy.insert(a); }
	cin>>y;
	FOR(i,0,y){ cin>>a; guy.insert(a); }
	if(guy.size()==N){
		cout<<"I become the guy."<<endl;
	}else
		cout<<"Oh, my keyboard!"<<endl;*/
	return 0;
}
